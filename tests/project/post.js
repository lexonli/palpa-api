const assert = require('chai').assert;
const api = require('../../api/project/index');

// template for a unittest
// it('', function () {});

describe('Test the create endpoint of project api', function () {
  it('create api should return status code 200 with valid request body', function () {});
});
